<!DOCTYPE html>
<html>
<head>
  <title>Blog</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" %>
  <%= stylesheet_link_tag "application","data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body>
<div class="d-flex flex-column flex-md-row align-items-center pb-3 mb-4 border-bottom">

  <h5 class="my-0 mr-md-auto font-weight-bold">Libraries</h5>
  <nav class="d-inline-flex mt-2 mt-md-0 ms-md-auto">
      <span class="btn btn-outline-primary"><%= link_to "View data", static_pages_landing_page_path %></span>
  </nav>
  <% if current_user %>
    <span class="btn btn-outline-primary"><%= link_to "Add library", new_library_path %></span>
  <% end %>
  <% if current_user %>
    <%= link_to "Log out", destroy_user_session_path, 'data-turbo-method': :delete, :class => 'btn btn-outline-primary' %>
  <% else %>
    <%= link_to "Log in ", new_user_session_path, :class => 'btn btn-outline-primary' %>
    <hr>
    <%= link_to " Register", new_user_registration_path, :class => 'btn btn-outline-primary' %>
  <% end %>
</div>

<div class="container">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>

</body>
</html>